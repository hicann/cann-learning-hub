1. D — A 和 C 都正确。仿真工作于复基带，不涉及射频载波；直接用独立随机种子在统计上与跳频到不同频率（信道独立）等价。
2. B — 阶段 (b) 接入请求中，发起者在 AccessRequestInfo 里声明 G/T 偏好和协商标志。
3. B — 深衰落的本质是多径信号到达时相位相反互相抵消，导致信号被削弱，不是噪声问题。
4. B — 连续失败阈值的作用是避免单次噪声波动误触发，只在信道条件持续恶化时才响应。
