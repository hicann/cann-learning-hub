# 5.3 参考答案

将 `M, N, NNZ` 设为 `64, 128, 100`，同步修改 indices、values、dense_shape、x2 和输出描述。
每个 COO 元素只贡献 `y[row] += value * x[col]`，因此可以直接累加 reference，无需构造稠密矩阵。

提交前必须检查：

```python
assert indices.shape == (NNZ, 2)
assert np.all((0 <= indices[:, 0]) & (indices[:, 0] < M))
assert np.all((0 <= indices[:, 1]) & (indices[:, 1] < N))
assert np.unique(indices, axis=0).shape[0] == NNZ
```

910B3/CANN 9.0 课程说明中的后端名称写作 AI CPU/tf_kernel；实际 `actual_backend` 字段保留 `AICPU/tf_kernel`，并设置
`aicore_accelerated=false`。Event 时间只比较本次 SpMV 的重复执行，不与 GEMM 横向比较。
