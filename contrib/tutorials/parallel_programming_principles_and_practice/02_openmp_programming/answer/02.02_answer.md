# 参考答案

例如 row_ptr=[0,2,3,5] 表示三行的非零区间。长度为 rows+1，因为相邻两个边界共同定义一行，末元素同时给出 nnz。


## 参考实验操作

实验必须基于 `src/openmp_spmv/`。用 `bash scripts/build.sh` 构建；固定矩阵和 repeat，以 `OMP_NUM_THREADS`、`OMP_SCHEDULE` 做单变量组合；串行结果作为正确性和 speedup 基线。

建议记录表：

| Threads | Schedule | CPU single | OpenMP | Speedup | Error |
|---|---|---|---|---|---|
|  |

分析时先判断退出码、正确性和可比性，再解释性能变化。若环境缺少 CANN、MPI 或多设备，应提交明确的诊断证据，不得把历史 CSV 冒充本机运行结果；参考答案不规定固定耗时。
