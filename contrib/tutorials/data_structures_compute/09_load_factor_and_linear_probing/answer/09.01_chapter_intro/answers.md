# 09.01 课后练习答案

1. `EMPTY` 表示该槽从未被占用，后续不可能存在同一探测链中的记录；`TOMBSTONE` 表示记录被删除，但后续槽仍可能保存有效记录，因此必须继续。
2. `α=n/M=128/256=0.5`。
3. value 可以合法地等于 0，只有独立 `hit_flags` 才能无歧义地区分命中与未命中。
4. `blockNum=min(1000,20)=20`，`queriesPerCore=ceil(1000/20)=50`。
