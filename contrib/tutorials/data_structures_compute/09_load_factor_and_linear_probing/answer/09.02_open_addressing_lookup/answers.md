# 09.02 课后思考答案

1. `TOMBSTONE` 不能截断探测链；如果把墓碑当作空槽，位于墓碑之后的有效 key 会被误判为未命中。
2. 装填因子升高会减少空槽并增加连续聚集的概率，因此平均、P99 和最大探测长度通常会增大。
3. SoA 允许先读取 state/key，只有确认命中后才读取 value，能够减少未命中路径的无效 value 带宽。
4. `probe_counts` 是算法访问次数；NPU 时间还包含哈希计算、分支、GM 访问和 Kernel 启动等成本，两者不能直接等同。
