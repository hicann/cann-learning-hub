# 10.01 课后练习参考答案

1. `parent[i] = p` 表示节点 `i` 的父节点是 `p`。根节点没有父节点，因此用 `-1` 表示。
2. FIFO 保持入队顺序和 BFS 层序；优先队列只在当前已经就绪的任务中改变选择顺序。子节点只有在父节点完成后才进入就绪队列，所以优先队列不能绕过父节点约束。
3. `queue_depth=2` 提供两个可复用的缓冲槽，使一个槽进行 `Compute` 或 `CopyOut` 时，另一个槽可以准备下一次 `CopyIn`，从而形成阶段重叠。
