# 05.01 练习答案

1. 在本实验使用的转置（入边）CSR 中，`row_ptr[v+1] - row_ptr[v]` 表示以顶点 `v` 为目标顶点的入边数量；在普通出边 CSR 中，它才表示顶点 `v` 的出边数量。
2. Pull 模式按目标顶点划分，每个 `dist_out[v]` 只有一个 AI Core 写入，因此不存在跨核写冲突。
3. 必须使用双距离缓冲区，因为同一轮应只读取 `d^(k)`；若原地更新，结果会依赖核执行顺序，不再是同步 Bellman-Ford。
