# 11.01 练习参考

1. `U=32` 时余数为 0，最后一个有效字的 32 位全部有效，所以掩码必须是 `0xFFFFFFFF`，不能写成 0。
2. `U=33` 需要 2 个有效字；为了使每行达到 32 Byte，物理跨度向上补齐到 8 个 `uint32` 字。
3. 位图适合全集有界且需要重复批量集合运算的场景；全集很大而集合极稀疏时，哈希集合通常更节省空间。
4. 集合交逐字独立，因此可以把批量位图展平成连续物理字数组进行多核切分。
