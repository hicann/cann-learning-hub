# 06.02 课后练习答案

1. `blockNum=min(1000,20)=20`，`queriesPerCore=ceil(1000/20)=50`。
2. 链地址法沿 `next` 指针逐节点比较，query（查询请求）命中位置和所在桶链长不同，循环次数也就不同。
3. 连续桶化方案每个 query 固定读取 2 个桶、检查 8 个槽，表数据搬运量为 `2×32B=64B`。
4. value（值）支持任意 `int32`，包括 0 和 -1；独立 found（命中标记）才能无歧义地表达未命中。
