1. ×
解析：直传模式的核心特点是零拷贝传输路径，数据直接在源端和目标端内存之间执行搬运，无需经过中间缓冲区。

2. √
解析：A3场景下使用HCCS传输时，不支持Host内存作为目标地址，如DH2、H2H、D2rH等方向。

3. B
解析：可以通过设置环境变量HCCL_INTRA_ROCE_ENABLE=1强制使用RoCE协议传输，适用于需要统一网络管理或部分硬件链路不支持的场景。

4. B
解析：A2场景下，机内默认使用HCCS传输，跨机默认使用RoCE传输。可通过设置环境变量`HCCL_INTRA_ROCE_ENABLE=1`强制使用RoCE协议。

5. A、B、C、D
解析：直传模式具有四大优势：零拷贝传输路径消除中间缓冲区拷贝开销；单边操作语义使发起方可直接读写对端内存；批量操作能力减少API调用开销；支持同步与异步双模式适应不同业务场景。