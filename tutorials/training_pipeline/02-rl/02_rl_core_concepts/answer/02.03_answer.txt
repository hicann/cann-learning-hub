1. √
解析：优势为正的 rollout（高于组平均）被强化，模型更倾向于产生类似输出；优势为负的（低于组平均）被抑制。

2. √
解析：PPO 的 clip 机制通过限制新旧策略的概率比（ratio）在 [1-ε, 1+ε] 范围内，防止每次更新幅度过大导致策略突变。

3. ×
解析：GRPO 的核心改进就是不需要 Critic 模型。它用组内平均奖励作为 baseline 来计算优势，省去了训练 Critic 的开销。

4. B
解析：PPO 中 Critic 模型用于估计每个状态的价值（value），从而计算优势（advantage = reward - value）。GRPO 用组内平均代替了这个功能。

5. B
解析：GRPO 的核心优势是不需要 Critic 模型，用组内平均代替。这减少了显存开销和训练复杂度，特别适合资源受限的场景。

6. A、B、D
解析：PPO clip 机制限制新旧策略的概率比（A）、防止策略突变（B）、保证每次更新幅度在安全范围内（D）。clip 机制本身不加速收敛（C 错误）。
