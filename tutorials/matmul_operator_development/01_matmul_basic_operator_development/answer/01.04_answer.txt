1. 错（不同 Kernel 特化版本的 Params 字段不同，如 Basic 无 workspace，StreamK 有 workspace）
2. 对（BlockMmad 根据 Layout 自动推导 transA/transB，不需要手动设置）
3. B（BlockEpilogueEmpty，适用于 Basic 非量化场景，不需要 AIV 端后处理）
4. D（ZNLayoutPtn 的 IsTrans=true, IsWeightNz=true）
5. L1 / L0（mL1/nL1/kL1 控制 L1 级搬运粒度，baseM/baseN/baseK 控制 L0 级计算粒度）