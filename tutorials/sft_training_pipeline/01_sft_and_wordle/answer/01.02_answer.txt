1. B
解析：G（Green）= 字母正确且位置正确；Y（Yellow）= 字母正确但位置错误；X（Gray）= 字母不在目标单词中。

2. D
解析：SFT 的核心目标是 format_reward——让模型学会按 `<guess>word</guess>` 格式输出，将其从基座模型的 ~0.2 提升到 ~1.0。correct_answer 等策略指标留到 RL 阶段。

3. √
解析：随机策略生成的对话已经包含正确的格式和 G/Y/X 反馈信息，足够完成 SFT 的格式对齐目标。最优策略留给 RL 阶段通过试错学习。
