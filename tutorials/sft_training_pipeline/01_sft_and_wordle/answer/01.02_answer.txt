1. B
解析：G（Green）= 字母正确且位置正确；Y（Yellow）= 字母正确但位置错误；X（Gray）= 完成 G 和按剩余字母计数的 Y 匹配后没有可用匹配。

2. D
解析：本课程用 format_reward 观察 `<guess>` XML 标签的存在性、位置与空白等格式要求；具体改善幅度必须由同口径的基座/SFT 评测得出。

3. A
解析：SFT 直接拟合多轮轨迹中的标签行为，学习根据历史反馈生成下一次猜测。策略质量由后续行为评测和环境奖励继续衡量。
