1. √
解析：28 层 × 2 个 RMSNorm（attention_norm + ffn_norm）= 56 个 RMSNorm 实例。

2. ×
解析：算子碎片化的主要代价是 kernel launch overhead（host→device 提交延迟累积）和 HBM 带宽浪费（每个小 kernel 独立读写 HBM）。模型参数量和显存占用不受影响。

3. B
解析：小模型（1.7B）在单卡上计算量有限，大部分时间花在等待 HBM 数据搬运上（memory-bound），而非 NPU 计算单元不够用。第 4 章通过融合算子减少 HBM 访存来改善。
