05.04 参考答案

1. EOS 只是消息结束标记，不是 packed sample 边界；一个多轮样本里可以出现多个 EOS。真正的新样本起点由 position 重新从 0 开始表达。
2. B。trainer 根据 position 编号重新变成 0 判断新样本开始。
3. 三段分别是 [0,1,2]、[0,1]、[0,1,2]，对应 cu_seq = [0,3,5,8]。
4. ×。IGNORE_INDEX 只影响 loss，不会阻止其他 token 读取该位置的 K/V。
