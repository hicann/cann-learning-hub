06.06 参考答案

1. A。两条受控路线是 `sft_qwen3_1_7b_wordle`（eager+packed 基线）与 `sft_qwen3_1_7b_wordle_tnd`（packed+varlen，TND 融合 Attention）。
2. √。trace 负责证明实际算子路径，关闭 profiler 的重复 wall-time 更适合看稳定速度。
3. ×。VarLen 只减少 Attention 中不需要的 pair，其他模块不会自动全部跳过。
4. A。原生 TPS 只统计容器位置，可能掩盖 padding 和监督量差异。
