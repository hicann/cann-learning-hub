08.03 参考答案

1. A。no-CP 为 `dp_shard=2, cp=1`；CP2 为 `dp_shard=1, cp=2`。两条路线固定相同的模型、数据、`seq_len=16384` 和 global batch，只调整 CP degree 与相应的 data-parallel shard。
2. √。本实验支持的结论是：在固定的 `S=16,384` workload 下，no-CP 不可运行而 CP2 可运行。CP 的收益是扩展长上下文容量；是否提升吞吐需要用独立的 profiler-off TPS 对照验证，不能由本实验推出。
