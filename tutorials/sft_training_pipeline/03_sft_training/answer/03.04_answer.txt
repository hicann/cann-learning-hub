1. ×
解析：`format_reward` 是分级格式分，`num_turns` 是交互长度指标；二者都不能单独证明模型理解了 G/Y/X 规则。需要查看固定评测集上的猜词行为和 `correct_answer` 等任务指标。

2. B
解析：这组结果只能说明当前评分器下格式分改善，而没有显示猜中率改善；不能仅凭两项指标断言是数据、策略还是环境因素导致。

3. D
解析：`load_environment()` 为 `format_reward` 显式设置 Rubric 权重 0.2；其余三个 reward function 使用默认权重 1.0，函数内部的部分分系数不等同于 Rubric 权重。
