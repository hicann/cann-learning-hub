1.修改核数后，会影响每个核处理的数据量发生变化，最终影响正常核和尾核处理的数据量。
2.将gen_data.py中indices = rng.randint(0, VAR_ROW, size=(ROW,)).astype(np.int32) 更改成 indices = np.arange(0, ROW, dtype=np.int32)，确保每个indices值不重复。
3.以scatter_add为例：var[varDataIdx] = updates[curID]; 可修改成 asc_atomic_add(var + varDataIdx, updates[curID]); 通过原子累加的方式进行输出计算。