1. ×
解析：水平融合要求两个分支共享同一上游节点的同一个输出数据。若读取的是不同输出，融合后仍需分别搬入两份数据，不能据此认定能够减少内存读写。

2. √
解析：CanFuse 框架负责检查收益、图结构和资源影响，Backend 还要确认 loop 轴映射与 group merge 均可行；两层条件都满足才能融合。

3. √
解析：最大融合个数按 Lowering 生成的 AscGraph 内部节点总数统计，而不是把两个外层 AscBackend 简单计为 2 个节点。

4. C
解析：默认最大融合个数为 64。

5. B
解析：候选节点融合后，若出现从融合节点出发并返回自身的依赖路径，计算图中就会形成环，因此不能融合。

6. A、B
解析：当前简化策略直接检查候选节点的 topo 序 ID 差值和水平融合后的输出内存；Python 函数名称和 NPU 卡号不是该策略的判断依据。

7. A、B、C
解析：不同 AscBackend 独立 Lowering，loop 轴 ID 可能需要重新映射；映射通过后仍需检查 group merge；rgroup 表示 Reduce 轴集合。Backend 条件不能替代 CanFuse 框架条件。