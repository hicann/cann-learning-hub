1. 对。已有目录可能用于特定分支或包含本地工作，Cell 只报告并复用当前状态；仅在目录不存在时克隆官方分支。

2. 错。课程主动关闭 checkpoint 保存；训练进度完成 3/3，且 rollout、log-prob、优势、Actor 更新和权重同步均成功，即可说明后端链路已经跑通。

3. A。固定的 PyTorch 2.12 wheel 使用 C++11 ABI，ATB 环境必须选择一致的库变体。

4. A、B、C。两个频率设为 -1 可移除保存和末次验证开销，同时保留 rollout、奖励、GRPO、Actor 更新与权重同步；训练负载不变。

5. A、B、C。首个异常、实际后端配置和当前 step 的输入规模共同支持定位；无关历史日志会干扰判断。
