1. 对。课程只设置三步上限，不缩小 batch、长度、采样数量或交互轮数。

2. 错。三步可以确认切换后的训练链路能够运行。比较性能还要固定负载，经过预热并重复运行。

3. A。课程只用 total training steps 限制训练规模；`SAVE_FREQ=-1` 与 `TEST_FREQ=-1` 只关闭保存和末次验证，不改变训练负载。

4. A、B、C、D。四个阶段从样本生成一路连接到参数更新，并让下一轮生成使用最新 Actor。

5. A、B、C。三步日志可以确认 TorchTitan 后端、原有数据流、FSDP2、TND 和权重同步已经运行；选项 D 还需要单独的受控性能对比。
