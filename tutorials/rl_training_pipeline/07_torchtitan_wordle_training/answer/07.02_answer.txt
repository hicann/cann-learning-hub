1. 对。两个目录分别隔离 Python 包和需要补丁的上游源码，职责不同。

2. 对。launcher 显式调用 `.venv/bin/python3`，确保固定依赖组合不受 Notebook kernel 或宿主 Python 包影响。

3. A。准备脚本使用 uv 0.12.0 创建并管理 TorchTitan-NPU 的独立 `.venv`。

4. A、B、C、D。安装脚本会依次准备固定源码、应用补丁、安装依赖并检查最终环境。

5. A、B、C。Cell 会复用已有代码和完整资产，只补齐缺失的数据或模型；不会在每次执行前删除已有权重。
