1. 对。三步训练将两张 NPU 用于 FSDP2 分片，配置为 DP shard 2、CP 1。

2. 错。CP 会减少每个 rank 处理的序列片段，但也会引入 all-to-all、填充和结果汇集开销，是否加速需要实测。

3. A。Ulysses all-to-all 在序列与注意力头之间交换分工，使每个 rank 以部分 heads 计算完整序列的注意力。

4. A、B、C。动态 packed token 流需要补齐、共享边界元数据，并在计算后汇集和去除新增 padding；奖励规则保持不变。

5. A、B、C、D。CP 的适用性取决于长序列显存压力、注意力头约束、通信成本以及与其他并行维度的组合方式。
