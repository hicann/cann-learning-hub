1. 对。环境反馈 token 不参与策略梯度计算，但会保留在对话上下文中，为模型下一轮生成提供 G/Y/X 信息。

2. C。第 2 次有效猜词时猜中，`length_bonus=1/2=0.5`；总奖励为 `1.0+0+0.5+0.2×1.0=1.7`。

3. C。20 个测试词各采样 5 次，因此一次完整验证会生成 `20×5=100` 条 rollout。

4. A、B、C、D。训练日志覆盖 Actor 状态、奖励与优势、序列长度以及性能指标；`critic/` 是指标命名空间，不表示本实验训练了 Critic 模型。

5. A、B、C。ROLLOUT_N 决定 GRPO 组大小，MAX_TURNS 限制交互轮次，ACTOR_LR 决定参数更新步长。NGPUS_PER_NODE 控制训练使用的 NPU 卡数和并行资源，不直接改变任务或算法行为。
