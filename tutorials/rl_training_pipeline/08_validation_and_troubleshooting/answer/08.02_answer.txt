1. 错。offload 在状态空闲时将其迁往主机内存，计算所需分片仍会在相应阶段加载到 NPU；它不是在主机上执行 Actor 计算。

2. 错。Wordle RL 总 step 耗时容易被 vLLM 多轮生成及有效 token 数波动主导，应单独比较训练阶段并按 token 归一化。

3. A。Context Parallel 沿序列维度分配激活与注意力计算，适合在长序列容量受限时进行扩展；其他三项不改变序列维度的并行容量。

4. A、B、C。三项分别覆盖 Actor 更新绝对耗时、按 token 归一化耗时和训练后端前向路径；单次总 step 不能隔离训练后端。

5. A、B、C、D。四项分别对应环境、TND 前向、rollout 和 Actor update。先找到最后成功的阶段，可以减少无关检查。
