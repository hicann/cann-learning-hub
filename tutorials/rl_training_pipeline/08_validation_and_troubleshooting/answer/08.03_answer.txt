1. 错。从后端选择、Actor/Ref 计算和权重同步日志就能判断切换是否生效；checkpoint 恢复用于长期训练中的状态管理。

2. 对。TND 通过变长元数据让注意力只处理有效 token；样本边界仍由 offsets 和 VarlenMetadata 明确表达，不能在 packed 计算中串联不同样本。

3. A。Actor 已更新而 rollout 策略未变化，首先应核对分片状态导出、参数映射和 vLLM `update_weights()`。

4. A、B、C、D。固定任务语义、工作负载、随机性与实验流程后，对比结果才更容易解释为训练后端带来的差异。

5. A、B、C、D。CP 是否适用同时取决于瓶颈、整除约束、变长数据路径和通信拓扑。
