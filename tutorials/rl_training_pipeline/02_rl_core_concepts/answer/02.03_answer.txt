1. 对。优势为正的 rollout（高于组平均）被强化，模型更倾向于产生类似输出；优势为负的（低于组平均）被抑制。

2. 错。GRPO 的核心改进就是不需要 Critic 模型。它用组内平均奖励作为 baseline 来计算优势，省去了训练 Critic 的开销。

3. B。PPO 中 Critic 模型用于估计每个状态的价值（value），从而计算优势（advantage = reward - value）。GRPO 用组内平均代替了这个功能。

4. B。GRPO 的核心优势是不需要 Critic 模型，用组内平均代替。这减少了设备内存开销和训练复杂度，特别适合资源受限的场景。

5. A、B、D。PPO clip 机制限制新旧策略的概率比（A）、防止策略突变（B）、保证每次更新幅度在安全范围内（D）。clip 机制本身不加速收敛（C 错误）。
