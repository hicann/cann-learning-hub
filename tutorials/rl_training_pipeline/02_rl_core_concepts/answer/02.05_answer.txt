1. 对。给同组奖励增加相同数值时，组内均值增加相同数值，标准差保持不变，因此标准化后的相对优势不变。

2. B。三条 rollout 的平均奖励为 0.8，奖励等于组内平均值时，未标准化优势为 0，标准化后也为 0。

3. B。`kl_loss_coef` 控制 KL loss 的约束强度。KL 持续快速增大时，需要检查当前 KL 约束是否足以限制策略偏离参考策略。

4. A、B、D。PPO 的组成部分包括 Critic 模型估计价值、clip 机制限制更新幅度和策略梯度。组内相对优势是 GRPO 的特征。

5. A、B、C、D。判断 RL 训练状态需要联合观察 entropy、kl_loss、grad_norm 和 response_length，不能只依据单一指标。
