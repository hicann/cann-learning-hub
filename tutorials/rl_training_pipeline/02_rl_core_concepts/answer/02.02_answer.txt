1. √
解析：在 RL 训练中，策略就是语言模型本身。给定输入后，模型输出一个 token 概率分布，RL 训练的目标就是优化这个分布，让模型更倾向于产生高奖励的输出。

2. ×
解析：参考策略（π_ref）是训练前的初始模型（SFT 产物），在整个 RL 训练过程中保持固定不变。它的作用是作为 KL 正则化的锚点，防止当前策略偏离太远。

3. √
解析：correct_answer 只有猜中才有奖励，是稀疏奖励；partial_answer 每轮都有部分匹配分数，是密集奖励。两者结合加速学习。

4. B
解析：保留参考策略的主要目的是通过 KL 正则化防止当前策略偏离初始模型太远，避免模型「跑偏」——为了获得高奖励而忘记原有的语言能力。

5. B
解析：GRPO 对同一个 prompt 生成多条 rollout，比较它们的奖励来计算相对优势。这是试错学习的核心方式——不需要告诉模型正确答案，只需要比较不同尝试的得分。
